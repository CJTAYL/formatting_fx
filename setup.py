from setuptools import setup, find_packages

setup(
    name='formatting_fx',
    version='0.1.0',
    description='Collection of functions to format data',
    author='Chris Taylor',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    zip_safe=False
)